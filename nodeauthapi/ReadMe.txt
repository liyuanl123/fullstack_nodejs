A small example to show how to use JWT(including sign and verify). This example is by the aid of Postman. 

In Postman, first send the request '/api/login'. A token is generated. After that, in each route 'api/posts', authorization field should be added. 